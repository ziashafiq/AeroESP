# =========================================================
# AeroESP - runtime dependencies
# =========================================================

# --- Core framework ---
Django==6.1
asgiref==3.12.1
sqlparse==0.5.5
tzdata==2026.3
python-dotenv==1.2.2

# --- Database ---
psycopg==3.3.4
psycopg-binary==3.3.4

# --- Production server / static files ---
# gunicorn does not run on Windows; skipped for local dev on Windows.
gunicorn==26.2.0; platform_system != "Windows"
whitenoise==6.11.0

# --- Security ---
django-axes==8.3.1
cryptography==50.0.1
cffi==2.1.1
pycparser==3.0

# --- Media / file handling ---
pillow==12.3.0
openpyxl==3.1.5
et_xmlfile==2.0.0

# --- AI provider (OpenAI SDK v3 + its HTTP stack) ---
# NOTE: httpx2 / httpcore2 are the real next-generation httpx packages
# (maintained under github.com/pydantic/httpx2) and are required by openai>=3.
openai==3.3.1
httpx2==2.12.0
httpcore2==2.12.0
h11==0.16.0
anyio==4.14.2
sniffio==1.3.1
idna==3.19
truststore==0.10.4
jiter==0.16.0
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.8.0
typing-inspection==0.4.4
typing_extensions==4.16.0
